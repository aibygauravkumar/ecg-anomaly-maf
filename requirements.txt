# Core — training, evaluation, and CLI scripts (main.py, detect.py)
torch>=2.0.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
scikit-learn>=1.3.0

# Streamlit web app only (app.py) — omit if you only need CLI
streamlit>=1.30.0
pillow>=10.0.0
neurokit2>=0.2.0
